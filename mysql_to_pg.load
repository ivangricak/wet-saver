LOAD DATABASE
     FROM mysql://root:@localhost/saver
     INTO postgresql://saver_user:vAUTkeArApLgyVCOmnWWma676v6zKRsQ@dpg-d35e3vogjchc73e05ceg-a.frankfurt-postgres.render.com:5432/saver_db?sslmode=require

 WITH include no drop, create tables, create indexes, reset sequences, data only

CAST
    type datetime to timestamptz,
    type date to date,
    type tinyint to smallint
;